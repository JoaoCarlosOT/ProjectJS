const path = require('path');

module.exports = {
  config: path.resolve('config', 'config.js'), // Caminho do config do banco
  'models-path': path.resolve('models'),       // Pasta dos models
  'seeders-path': path.resolve('seeders'),     // Pasta dos seeders
  'migrations-path': path.resolve('migrations')// Pasta das migrations
};
